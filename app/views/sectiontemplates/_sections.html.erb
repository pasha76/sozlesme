<% for tmp in @templates %>
    <tr class="odd gradeX">
      <td>
        <div class="div-text">
          <%= cmp_editable_text(tmp, "name")%>
        </div>
      </td>

      <td>
        <div class="div-text">
          <%= cmp_editable_text(tmp, "subject")%>
        </div>
      </td>
      <td><%= User.find(tmp.user_id).name %></td>
      <!--<td class="center"><%= tmp.created_at.strftime("%d/%m/%Y") %></td>-->
      <td class="center"><a class="btn btn-xs btn-success" href="/sectiontemplates/<%= tmp.id %>/deactivate">Kaldır</a>
      </td>

    </tr>
<% end %>